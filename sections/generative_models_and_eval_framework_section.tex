\section{Benchmark Models And Evaluation Framework}

This section defines the benchmark protocol used to evaluate whether optical prescription data can be modeled by modern generative methods and whether optics-aware processing improves generated design quality. We first evaluate unconditional generation because it isolates distribution learning: the model must generate plausible lens prescriptions and design specifications without being given a target condition at sampling time. Conditional target satisfaction is treated separately for the proposed conditional model.

\subsection{Benchmark Models}\label{subsec:off_shelf_uncond_gen}

The benchmark suite uses mixed-type tabular generative models as the primary unconditional baselines, including TabDDPM, STaSy, CoDi, and TabSyn \cite{Kotelnikov2023TabDDPM,Kim2023STaSy,Lee2023CoDi,Zhang2024TabSyn}. These methods are appropriate baselines because the model-facing lens representations are stored as mixed numerical and categorical tables. At the same time, they do not natively encode optical surface ordering, variable surface count, aperture-stop structure, or ray-trace validity constraints. This makes them useful for testing how much benefit comes from the proposed data representation and post-processing pipeline.

For each model family, experiments are organized across the dataset variants in Table~\ref{tab:dataset_variants_splits} and across the untuned and tuned model-facing representations. Model-specific processors are allowed to perform required formatting, imputation, and normalization, but these steps are recorded separately from the common representation layer. This separation is important because the common data representation is intended to define the optical variables, while model-specific processors define the input format required by a particular generative model implementation.

\begin{table}[t]
\centering
\small
\caption{\textbf{Dataset variants and parent-safe splits.} Train, validation, and test counts include both patent-derived and synthetic rows.}
\label{tab:dataset_variants_splits}
\begin{tabular}{lrrrr}
\toprule
\textbf{Variant} & \textbf{Train rows} & \textbf{Val rows} & \textbf{Test rows} & \textbf{Total rows} \\
\midrule
Patent-only & 1,003 & 125 & 125 & 1,253 \\
Patent+synth-250k & 251,003 & 111,111 & 112,642 & 474,756 \\
Patent+synth-500k & 501,003 & 111,111 & 112,642 & 724,756 \\
Patent+synth-full & 902,580 & 111,111 & 112,642 & 1,126,333 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\footnotesize
\caption{\textcolor{red}{[placeholder table]} \textbf{Benchmark training protocol.} This table will be filled with the final model list, dataset variants, representation settings, and sampling counts used in the benchmark runs.}
\label{tab:benchmark_protocol}
\begin{tabular}{p{0.18\linewidth}p{0.20\linewidth}p{0.25\linewidth}p{0.25\linewidth}}
\toprule
\textbf{Model} & \textbf{Representation(s)} & \textbf{Dataset variant(s)} & \textbf{Role} \\
\midrule
TabSyn & Tuned, untuned & Patent-only, 250k, 500k, full & Main latent-diffusion tabular baseline \\
TabDDPM & Tuned, untuned & Planned benchmark variants & Diffusion tabular baseline \\
STaSy & Tuned, untuned & Planned benchmark variants & Score-based tabular baseline \\
CoDi & Tuned, untuned & Planned benchmark variants & Mixed-type contrastive diffusion baseline \\
SMOTE-style interpolation & Tuned or untuned reference & Selected variants & Non-neural diversity/memorization reference \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Evaluation Stages}

Evaluation is separated into stages so that local generated-prescription quality is not conflated with CodeV/LLESP simulation outcomes. Before CodeV results are available, generated samples can be evaluated for table validity, material validity, geometric plausibility, distribution matching, diversity, novelty, and LLESP-readiness after deterministic post-processing. After CodeV/LLESP result files return, the same samples can be evaluated for simulation error rate and optical performance metrics.

For unconditional generation, design conditions such as EFL, EPD, and maximum field angle are treated as generated design specifications. Their distributions are compared against reference data, but they are not treated as target-satisfaction metrics because no requested target is supplied to the unconditional model at sampling time. For conditional generation, target satisfaction becomes a separate metric family.

\subsection{Metric Families}

Table~\ref{tab:evaluation_metric_groups} summarizes the metric families used to support the benchmark claims. Distribution and diversity metrics are computed on prescription and design-condition columns, excluding optical performance columns such as spot size and encircled-energy metrics. Optical performance is evaluated only after ray tracing or CodeV/LLESP simulation has produced valid result files; detailed post-processing attrition counts can be reported in the appendix if needed.

\begin{table}[t]
\centering
\footnotesize
\caption{\textcolor{red}{[placeholder table]} \textbf{Evaluation metric groups.} The final paper will report completed metrics for generated prescription quality and CodeV/LLESP-validated optical performance.}
\label{tab:evaluation_metric_groups}
\begin{tabular}{p{0.17\linewidth}p{0.31\linewidth}p{0.15\linewidth}p{0.22\linewidth}}
\toprule
\textbf{Metric group} & \textbf{Examples} & \textbf{Stage} & \textbf{Claim supported} \\
\midrule
Distribution matching & Marginal statistics, pairwise correlations, $\alpha$-precision/$\beta$-recall & Pre-CodeV & Fidelity to reference prescriptions \\
Diversity and novelty & Generated-to-train NN distance, generated-to-generated NN distance, test-to-generated coverage & Pre-CodeV & Non-collapse and held-out coverage \\
Duplicate checks & Rounded duplicates, generated rows matching training rows & Pre-CodeV & Publication defensibility and leakage control \\
Validity and feasibility & Material no-match rate, structural surface validity, geometry issues, LLESP-readiness & Post-processing / pre-CodeV & Usable generated prescriptions \\
CodeV/LLESP outcomes & \texttt{!error} rate, simulation success/failure categories & CodeV-dependent & Simulation robustness \\
Optical performance & SPO67, SPO100, EE50, EE90, axial nominal performance & CodeV-dependent & Optical quality of generated designs \\
Ray-tracing benefit & Paired before/after CodeV success rate and optical performance metrics & CodeV-dependent & Physics-based post-processing benefit \\
Condition satisfaction & Target EFL/EPD/field error & Conditional only & Target-directed generation \\
\bottomrule
\end{tabular}
\end{table}

For diversity and novelty, the immediate metric pass compares samples within the same surface-count group using robust-scaled prescription-space distances. This avoids treating padded inactive surface slots as ordinary features. A topology-aware cross-surface-count distance can be used as a stronger analysis when finalized, but the same-surface-count nearest-neighbor metrics provide a simpler first-pass memorization and coverage check.
